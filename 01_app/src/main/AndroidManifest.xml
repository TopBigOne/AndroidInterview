<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.interview">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.life.DLifeActivity"
            android:exported="false" />
        <activity
            android:name=".activity.life.CLifeActivity"
            android:exported="false" />
        <activity
            android:name=".activity.life.BLifeActivity"
            android:exported="false" />
        <activity
            android:name=".activity.life.ALifeActivity"
            android:exported="false" />
        <activity android:name=".activity.BaseActivity" />

        <activity android:name=".event.OnTouchActivity" />

        <activity
            android:name=".activity.ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts URIs that begin with "example://gizmos” -->
                <data
                    android:host="ya"
                    android:scheme="jar" />
            </intent-filter>
        </activity>
    </application>

</manifest>